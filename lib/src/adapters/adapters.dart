/// Contract-to-product adapters and DTO mappers.
///
/// Pure Dart only — no persistence dependencies.
/// Storage-specific adapters (e.g. SharedPreferenceAdapter) live in example/lib/.
export 'taiyi_contract_adapters.dart';
