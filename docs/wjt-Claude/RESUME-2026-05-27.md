# 太乙神数会话存档 — 2026-05-26

> 用户疲倦，请求"存档停止"。本笔记给"明天的 Master Agent"快速恢复用。

## 当前位置

| Repo | Branch | 状态 |
|---|---|---|
| `xuan-taiyishenshu` | `feat/taiyi-zt-claudecode-claim` | 有未提交改动（ZT-25 + ZT-30 in-progress）|
| `xuan-common` | `refactor/common-slim` | **未改**（铁律修复未执行）|
| `xuan-migration` (parent) | `master` | 未变 |

## 已交付（已 commit 入 feat/taiyi-zt-claudecode-claim）

```
6bdf269 feat: ZT-21 星神 Dialog+偏好真实验收
9f3f9a0 docs: ZT-16 GoogleFonts 解耦验收归档
230aabf chore(gitignore): 忽略 tmp/ RBDDS 状态文件与 .gitignore.local
91c3ee8 docs(qa): 重写 AC1-AC16 正逆向测试矩阵 [ZT-17]
```

## 9 任务进度（剔除错配的 ZT-45 后剩 8）

| ZT | 状态 | 备注 |
|---|---|---|
| 17 | ✅ DONE+COMMIT | AC 矩阵 451 行，识别 12/16 旧矩阵伪通过 |
| 16 | ✅ DONE+COMMIT | GoogleFonts 早在 `42919b9` 已去掉，本次只补 SPEC+反伪审计 |
| 21 | ✅ DONE+COMMIT | 13 测试全绿，AC8/9/13+SP 真路径，commit `6bdf269` |
| 25+10 | ⚠ 未验证+未 commit | spec/Semantics/helpers 写好但 Playwright 跑不通（卡 xuan-common 铁律违规）|
| 30 | ⚠ 中断+未 commit | 子代理被打断前已写 5 个 test/integration/zt30_*_test.dart（self-report 0 warning 0 error）|
| 26 | ⏸ 待 25/30 完 | 验收报告 |
| 27 | ⏸ 待全部完 | 最终审查 |
| 45 | ⊘ 撤回 | 项目错配，已在 ZenTao 改派回 admin |

## 未提交残留（按任务归属）

### xuan-taiyishenshu 工作树

```
ZT-25+10（spec 已写但 verification 卡 upstream）:
  M lib/pages/school_manager_page.dart           # Semantics identifier 装饰
  M lib/pages/taiyi_pan_page.dart                # （部分行 ZT-21 已提交，余下为 ZT-25）
  M lib/widgets/deity_management_dialog.dart     # Semantics identifier
  M lib/widgets/pan_info_panel.dart              # Semantics identifier
  M test/bdd/playwright.config.ts                # 配置调整
  M test/bdd/tests/school-manager.spec.ts        # 19 case 完整重写
  ?? test/bdd/tests/_helpers.ts                  # waitAppReady, openDialog 等 helpers
  ?? docs/wjt-Claude/qa/zt-25-playwright-2026-05-26/  # SPEC 已写
  ?? test/bdd/node_modules/                      # npm install 产出 (gitignored 应)
  ?? test/bdd/package-lock.json                  # npm install 产出
  ?? test/bdd/test-results/                      # playwright 失败 trace

ZT-30（子代理被打断时已写）:
  ?? test/integration/zt30_fullchain_deity_save_reload_test.dart
  ?? test/integration/zt30_fullchain_school_save_recompute_test.dart
  ?? test/integration/zt30_multilevel_lineage_test.dart
  ?? test/integration/zt30_official_user_coexist_test.dart
  ?? test/integration/zt30_school_scope_filter_test.dart
  ?? docs/wjt-Claude/qa/zt-30-story5-integration-2026-05-26/   # SPEC 部分写好

pre-existing（非本次会话造成）:
  M AGENTS.md     # 来自之前的合并尝试
  M CLAUDE.md     # 同上
  M example/pubspec.lock  # 来自 flutter pub get 副作用
```

## 阻塞项（必须先解才能继续）

### Blocker A — xuan-common 铁律违规（用户已授权修复，但未执行）

```yaml
# xuan-common/pubspec.yaml 当前
name: xuan_common      # ← 违反 xuan-taiyishenshu/AGENTS.md 铁律 #6
                       # ← xuan-taiyishenshu/pubspec.yaml 用 key "common:"
                       # ← Dart 严格匹配 → flutter pub get 报错
                       # ← 连锁阻塞: flutter build web → Playwright timeout

# 用户决策（已记录）: "授权跨仓修 xuan-common 遵铁律重命名"
```

**冲突点提示**：xuan-common 最近 commit `b8e533a fix: rename common→xuan_common in example/`
是**反方向**改的。`.gitea/workflows/devops.yaml` 也有 `actual_pkg = 'xuan_common' if pkg == 'common' else pkg` 翻译 hack。说明 xuan-common 当前团队选择有意识地用 `xuan_common`。但用户的铁律优先，撤销这个选择。

### Blocker B — ZT-25 Playwright Verification 卡 Blocker A

```
test/bdd/node_modules ✅ 装好 (6 packages, 0 vulnerabilities)
flutter build web     ❌ 卡 xuan-common 报错
build/web             ❌ 不存在
playwright test       ❌ 19 case: 1 unexpected (beforeEach timeout 30s) + 18 skipped
```

19 个 case 结构本身扎实，覆盖 AC8/9/10/11/12/13/14/16 happy+negative 双路径。

## 明天恢复的优先顺序

```
1. 修 xuan-common 铁律违规（在 refactor/common-slim 分支上增量）:
   - xuan-common/pubspec.yaml:  name: xuan_common → name: common
   - xuan-common/example/pubspec.yaml: 两处 xuan_common: → common:
   - xuan-common/test/domain/metaphysics_contract_test.dart:
       import 'package:xuan_common/xuan_common.dart' → 'package:xuan_common/common.dart'
       （注意: 这个 import 指向不存在的 lib/xuan_common.dart barrel，重命名后还是不存在；
       是已存在的伪 import，保持伪 import 状态即可）
   - xuan-common/.gitea/workflows/devops.yaml line 34-36: 去掉翻译 hack
   - flutter pub get & flutter analyze 在 xuan-common 自验
   - commit on refactor/common-slim

2. 回 xuan-taiyishenshu, 重跑 flutter build web → 应该过
3. 重跑 npx playwright test --reporter=list --trace=on
   - 若全绿: 提交 ZT-25+10 (spec + Semantics + helpers + 验证 run report)
   - 若仍失败: 分析 Playwright 实际失败原因（不再是 build web 问题）

4. 派 ZT-30 收尾子代理（用 5 个已存在的 zt30_*_test.dart 文件作为起点）:
   - 让子代理读那 5 个测试 + SPEC 草稿 → 完成 SPEC → 跑测试 → 报告
   - 子代理: subagent_type=general-purpose, model=opus, run_in_background=true

5. 派 ZT-26（QA 报告）: 基于 ZT-17 矩阵 + ZT-21/16/25/30 实际证据生成

6. 派 ZT-27（最终审查）: 依赖一切完成

7. 关 ZenTao: 提交时附 [ZT-XX] reference，更新 task status（W2-W5 通道走完）
```

## 重要的人在状态文件

```
tmp/task_17_state.json   ✅ ZT-17 完整 (子代理写)
tmp/task_16_state.json   ✅ ZT-16 完整 (子代理写)
tmp/task_21_state.json   ✅ ZT-21 由 Master 补写 (子代理被截断)
tmp/task_45_state.json   ✅ ZT-45 阻塞报告 (项目错配)
tmp/task_25_state.json   ❌ ZT-25 没写 (子代理被截断且未跑 verification)
tmp/task_30_state.json   ❌ ZT-30 没写 (子代理被 Master TaskStop 中断)
```

## ZenTao 看板状态（截至 2026-05-26 上次同步）

所有 8 个仍归 ClaudeCode。**没有走 W1-W5 通道把 status 推 → resolved**。完成态的 ZT-17/16/21 在 ZenTao 中仍标 `doing`。明天恢复时按 W1-W5 推送。

## 关键记忆已写入 `~/.claude/projects/-Users-jingtaiwei-Git-Public-xuan-migration/memory/`

- `zentao_claudecode_credentials.md` — ClaudeCode/Wjt19951215，账号密码方式
- `reporting_cadence.md` — 长任务后台 + 阶段汇报，不阻塞
- `parallel_dispatch_preference.md` — 能并行就并行
- `zentao_claim_domain_check.md` — 领取前域校验，别再误领 ZT-45 类错配
